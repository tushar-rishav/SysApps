<script>
    var i=1,c=0;
    for (i=2;i<6;i++)
    {
      document.getElementById("s"+i).style.backgroundColor="rgb("+Math.floor(Math.random()*255)+","+Math.floor(Math.random()*255)+","+Math.floor(Math.random()*255)+")"

      document.getElementById("s"+i).style.width=screen.availWidth*0.98+"px";
      document.getElementById("s"+i).style.height=screen.availHeight+"px";
    }

    $(function() { $(".lavaLamp").lavaLamp({ fx: "backout", speed: 700 });  });

    // Set options
    var options = {
      offset: '#showHere',
      classes: {
        clone:   'banner--clone',
        stick:   'banner--stick',
        unstick: 'banner--unstick'
      }
    };

    // Initialise with options
    var banner = new Headhesive('.banner', options);

    // Headhesive destroy
    // banner.destroy();

    window.sr = new scrollReveal();

    // for modal in index page
    $(function () { $('#myModal').modal({
      keyboard: true
    })});


</script>
